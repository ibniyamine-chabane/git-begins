

 pour cloner un repo c'est la commande :

	git clone + (url https ou ssh du repo)

pour ajouté un fichier au repo : 

	git add -A 

pour commenter les l'ajout du git add : 

	git commit -m "notre commentaire"

et pour push dans la branche main :

	git push origin main


